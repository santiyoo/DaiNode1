export default function sumar(num1, num2){
    return num1 + num2;
}

export const PI = 3.14;

export let dividir = (a, b) => {
    let total = 0;
    total = a / b;
    return total;
}

export let potencia = a => a * a;